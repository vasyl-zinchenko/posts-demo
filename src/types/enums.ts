export enum ErrorMessages {
  LoadPosts = "Failed to load posts",
  LoadPostById = "Failed to load post",
  UpdatePost = "Failed to update the post",
  AddComment = "Failed to add comment",
  DeletePost = "Failed to delete the post",
}

export enum SuccesMessages {
  UpdatePost = "The post has been updated successfully",
  AddComment = "Comment added successfully",
}

export enum PostType {
  AllPosts = "All posts",
  UserPosts = "Your posts",
}

export enum SortType {
  New = "new",
  Old = "old",
}
